import React from "react";

const Title = (props) => {
  return (
    <div className="title-1">
      <h2>{props.name}</h2>
      <p>Upload and View Images</p>
    </div>
  );
};

export default Title;
